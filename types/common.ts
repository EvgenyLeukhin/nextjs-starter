export type TId = string;

export type TNavLink = {
  title: string;
  href: string;
};

export type TSocial = {
  title: string;
  link: string;
};

export enum Statuses {
  primary = 'primary',
  secondary = 'secondary',
  success = 'success',
  warning = 'warning',
  danger = 'danger',
}
